<section class="has-bg p-0 d-flex main-hero" style="background-image: url(https://www.24k.com.sg/workz/johnny/mist-valley-shop/v2/images/hero-cart.jpg);">
  <div class="caption-holder">
    <h2>{{ 'customer.account.title' | t }}</h2>
  </div>
</section>
<!-- end section -->

<section class="subpage logon">
  <figure class="subsection">
    <div class="container">
      <div class="section-body">
        <p><a href="{{ routes.account_logout_url }}">{{ 'customer.log_out' | t }}?</a></p>
        <p class="lead">Welcome back, <strong>Peter Yap</strong></p>
        <div class="row mt-5">
          <div class="col-md-8 pr-lg-5">
            {%- if customer.orders.size > 0 -%}
            <h2>{{ 'customer.orders.title' | t }}</h2>
            <div class="table-responsive">
              <table class="table">
                <thead>
                <th>Order Number</th>
                <th>Date</th>
                <th>Status</th>
                <th>Total</th>
                <th></th>
                </thead>
                <tbody>
                {%- for order in customer.orders -%}

                  <tr>
                    <td><a href="{{ order.customer_url }}">{{ 'customer.orders.order_number' | t }}</a></td>
                    <td> {{ order.created_at | time_tag: format: 'date' }}</td>
                    <td>{{ order.financial_status_label }}</td>
                    <td>{{ order.total_price | money_with_currency }}</td>
                    <td><a href="{{ order.customer_url }}">Order Detail</a></td>
                  </tr>


                {%- endfor -%}


                </tbody>
              </table>
            </div>

            {%  else %}
              <h2 class="mt-5">{{ 'customer.orders.title' | t }}</h2>
              <p>{{ 'customer.orders.none' | t }}</p>
            {%  endif %}

          </div>
          <div class="col-md-4 pl-lg-5">
            <h2>{{ 'customer.account.details' | t }}</h2>

            {{ customer.default_address | format_address }}

            <a href="{{ routes.account_addresses_url }}">
              {{ 'customer.account.view_addresses' | t }} ({{ customer.addresses_count }})
            </a>
            <!--
            <address>
              Peter Yap<br/><br/>
              My company<br/>
              Blk 888, <br/>Ang Mo Kio Avenue 4,<br/>88888 Singapore
            </address>
            <a href="manage-address.php" class="btn btn-site">Edit Address</a>
            <h2 class="mt-5">Primary address</h2>
            <address>
              No addresses are currently saved
            </address>
            <a class="btn btn-site" href="manage-address.php">Manage address</a>
            -->
          </div>
        </div>
      </div>
    </div>
  </figure>
</section>

